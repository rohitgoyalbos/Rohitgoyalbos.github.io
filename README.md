# Rohitgoyalbos.github.io

This repository hosts a simple React prototype referencing the design system described on Zeroheight.

Open `index.html` in a browser and log in using **guest/guest** to try the example.
